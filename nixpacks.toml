[phases.setup]
nixPkgs = ["nodejs_20"]
aptPkgs = ["ffmpeg", "yt-dlp", "ca-certificates"]

[phases.install]
cmds = ["npm ci --omit=dev || npm install --omit=dev"]

[start]
cmd = "node server.js"

[variables]
NODE_ENV = "production"
